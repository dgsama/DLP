
	int ->2
	real->4
	char->1

OFFSET
Global variables, Fields
	suma de las previas

Local variables, parameters
	se parte de un numero, resta de las previas
	
	ej:
	
	partes de 10
	
	int a offset = 8
	char b offset = 6
	real c offset = 2
	
	
	
	
Programa ejemplo para conversion de tipos

char c;

void f(int i){
c=i*2.0;
}




examen mayo

a>>b>>c -> a>b && b>c. Segundo ejercicio lo mismo pero con cortocircuito si la primera falla no se evalua la siguiente.

examen junio

Switch de enteros.
operador ternario.

examen febrero

landa expresions mas menos
array.apply(i -> i+1)
			(i -> 4)
			(i -> f(i))